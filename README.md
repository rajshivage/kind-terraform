## kind-terraform

### Clone the repo

`git clone git@github.com:<username>/kind-terraform.git`

### Run below commands to build kind k8s cluster

```
cd kind-terraform
terraform init
terraform plan
terraform apply
```
